// single line comment(cntrl+forwardslash)

// declaring thevariable with let keyword

let x

//assigning the value to the variable

x=1023

console.log(x) //1023


// Intalization

let flower = 'rose'

console.log(flower)

// Reintialization(reintialization can happens only for same dtat types)

 flower = 10.23 // if we want to try to assign a different data type value to the variable it will show complietime errors but it will 
                //stop code execution and results or values retained in terminal panel//

console.log(flower)

//redeclaring the variable with value

//redeclartion wont be happen for the let keyword

let flower = 'white rose'

console.log(flower)

